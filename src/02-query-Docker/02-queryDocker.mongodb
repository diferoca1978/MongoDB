//* Use the reservated woord (use) to select the colection that you want connect.
use('Platzi-store-db');

//* This the query to document

db.products.find({ price: 1200 });
//db.zips.find({ state: 'NY' }).count();
